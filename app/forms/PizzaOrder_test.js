'use strict';

describe('myApp.forms module', function() {

  beforeEach(module('myApp.forms'));

  describe('forms controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var view1Ctrl = $controller('View1Ctrl');
      expect(view1Ctrl).toBeDefined();
    }));

  });
});